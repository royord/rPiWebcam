# Core runtime dependencies for rPiWebcam
# Install with: pip install -r requirements.txt
#
# Note: picamera2, libcamera, and libarchive-c also require system packages
# on Raspberry Pi OS. Install those first:
#   sudo apt install python3-picamera2 python3-libcamera libarchive13

Flask==3.1.3
Pillow==12.2.0
picamera2==0.3.36
paramiko==5.0.0
scp==0.15.0
netifaces==0.11.0
getrpimodel==0.1.26
configobj==5.0.9
requests==2.34.1
sunset>=1.1
