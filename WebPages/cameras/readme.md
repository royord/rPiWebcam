# Cameras

This directory is created as the supposed destination for images captured by the application. It is assumed that with in
this directory will be a folder per camera as well so that the web site will function on its own.